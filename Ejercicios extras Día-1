/*sort-array-by-parity*/

let numbers = [3, 1, 2, 4];

function parity(nums) {
  let newNumbers = [];
  for (let num of nums) {
    if (num % 2 === 0) {
      newNumbers.unshift(num);
    } else {
      newNumbers.push(num);
    }
  }
  return newNumbers;
}
console.log(parity(numbers));

